<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Edit a travel spot</title>
	</head>
	<body>
		<h1>Edit a travel spot</h1>
		<form action="/locations/<%= location._id %>?_method=PUT" method="POST">
			<div>
				<label for="title">Add location title</label>
				<input
					type="text"
					name="campground[title]"
					id="title"
					value="<%= location.title %>"
				/>
			</div>
			<div>
				<label for="location">Add spot location</label>
				<input
					type="text"
					name="campground[location]"
					placeholder="Spot location"
					id="location"
					value="<%= location.location %>"
				/>
			</div>
			<div>
				<label for="description">Add a description</label>
				<input
					type="text"
					name="campground[description]"
					placeholder="Description"
					id="description"
					value="<%= location.description %>"
				/>
			</div>
			<div>
				<label for="price">Add location price for a day and night</label>
				<input
					type="number"
					step="0.01"
					name="campground[price]"
					placeholder="Price"
					id="price"
					value="<%= location.price %>"
				/>
			</div>

			<button type="submit">Edit location</button>
		</form>
		<a href="/locations/<%= location._id %> ">Go back to location</a>
	</body>
</html>
